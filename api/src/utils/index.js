const sizeList = ['small', 'medium', 'big'];

const statusList = ['transito', 'adoptado'];

const colorList = ['white', 'black'];

const speciesList = ['docg', 'cat'];

module.exports = {
    sizeList,
    statusList,
    colorList,
    speciesList,
};
