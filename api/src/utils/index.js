const genderList = ['Macho', 'Hembra'];

const sizeList = ['Pequeño', 'Mediano', 'Grande'];

const statusList = ['Rescatado', 'Adopcion', 'Adoptado'];

const speciesList = ['Perro', 'Gato'];

module.exports = {
    genderList,
    sizeList,
    statusList,
    speciesList,
};
